# internal/config/ (opcional)

`@Configuration` que SO este modulo usa: beans, propriedades
(`@ConfigurationProperties`), customizers locais.

Configuracao global da aplicacao vive em `infra/config/`, nao aqui.

Remover esta pasta se o modulo nao precisa de configuracao propria.
