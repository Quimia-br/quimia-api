# test/.../domain/

Testes dos modulos de negocio — estrutura espelhada do codigo de
producao. O pacote do teste e o MESMO do codigo testado, dando acesso
a classes `package-private` sem precisar torna-las `public`.

Um subdiretorio por modulo, copiando a arvore de `main/.../domain/`.
