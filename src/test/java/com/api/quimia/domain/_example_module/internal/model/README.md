# test/.../model/

Testes do modelo de dominio — comportamento de entidades ricas
(`order.cancel()`, `wallet.debit(amount)`) e Value Objects.

Convencao de nome: `<Entity>Test` (`OrderTest`).

Sem Spring, sem mocks de framework — testes de logica pura, rapidos.
